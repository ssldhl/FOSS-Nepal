<li>
  <%= gravatar_for user%>
  <%= link_to user.username, user %>
  <% if user.admin? %>
  	| Administrator
  <% end %>
  <% if user.mod? %>
    | Moderator
  <% end %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "delete", user, method: :delete, data: { confirm: "You sure?" } %>
    | <%= link_to "Admin(Click to alter)", :controller => "users", :id => user.id, :action => "alter_role", :method => :put%>
    | <%= link_to "Moderator(Click to alter)", :controller => "users", :id => user.id, :action => "def_mod", :method => :put%> 
  <% end %>
</li>